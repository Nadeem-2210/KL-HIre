/**
 * Job domains that skip the coding round (role does not require coding assessment).
 * Keep in sync with frontend `DOMAIN_SKIPS_CODING` in utils/constants.js
 */
module.exports.DOMAIN_SKIPS_CODING = 'Business Analyst';
